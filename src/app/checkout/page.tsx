import React from "react";
import Checkout from "@/components/Checkout";

export const metadata = {
	title: "WashingTon Laundry - Checkout",
};

const index = () => {
	return (
		<>
			<Checkout />
		</>
	);
};

export default index;
