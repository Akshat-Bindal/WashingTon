import React from "react";
import Contact from "@/components/Contact";

export const metadata = {
	title: "washingTon Laundry - Contact",
};

const index = () => {
	return (
		<>
			<Contact />
		</>
	);
};

export default index;
